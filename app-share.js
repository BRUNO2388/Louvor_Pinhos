async function gerarImagemECompartilhar(){const el=document.getElementById('folha-timbrada');try{const canvas=await html2canvas(el,{backgroundColor:'#fff',scale:2});canvas.toBlob(async blob=>{const file=new File([blob],'relatorio-louvor.png',{type:'image/png'});if(navigator.canShare&&navigator.canShare({files:[file]}))await navigator.share({files:[file],title:'Relatório Grupo de Louvor'});else{const a=document.createElement('a');a.href=canvas.toDataURL('image/png');a.download='relatorio-louvor.png';a.click()}},'image/png')}catch(e){alert('Não foi possível gerar a imagem.')}}
function enviarTextoWhatsApp(){const data=document.getElementById('ensaio-data').value,itens=Object.values((window.todosOsDados||{})[data]||{});if(!itens.length)return alert('Sem registros nesta data.');let txt=`*Grupo de Louvor - ICM PINHOS*\n*${formatData(data)}*\n\n`;itens.sort((a,b)=>(a.nome||'').localeCompare(b.nome||'','pt-BR')).forEach(m=>txt+=`${m.status==='presente'?'✅':m.status==='atrasado'?'🟡':'❌'} ${m.nome} - ${m.status==='presente'?'Presente':m.status==='atrasado'?'Atrasado':'Faltou'}${m.motivo?' ('+m.motivo+')':''}\n`);window.open('https://wa.me/?text='+encodeURIComponent(txt),'_blank')}

function renderTudo(){if(!window.usuarioAtual)return;renderizarListaCadastrados();montarRascunho();renderChamadaSalva();gerarFolhaDiaria();renderHistorico();carregarDashboard();if(window.isAdmin){carregarRolesAdmin();renderAuditoria();renderPerfis()}}